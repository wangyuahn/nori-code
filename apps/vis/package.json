{
  "name": "@nori-code/vis",
  "version": "0.1.1",
  "private": true,
  "description": "Debug visualization tool for Nori Code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "pnpm --filter @nori-code/agent-core... build",
    "predev": "pnpm run build:deps",
    "dev": "node scripts/dev.mjs",
    "build": "pnpm run build:deps && pnpm --filter @nori-code/vis-server build && pnpm --filter @nori-code/vis-web build && node scripts/copy-web-dist.mjs",
    "prestart": "pnpm run build",
    "start": "node server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
