---
"nori-code": patch
---

Add conversation-map session controls for filters, selection, tags, and per-card actions. Select sessions on the map, then use a card or the toolbar to open, stop, tag, unmount, or delete them.
