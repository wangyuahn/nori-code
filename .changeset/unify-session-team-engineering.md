---
"nori-code": minor
---

Make department members the same Sessions as conversation-map cards, so hire, chat, Discuss, and `/map` share one forest. Hire with TeamCreate or the map; open a card to work in that session. In the terminal, `/team` and `/map` keep the live department snapshot, and Ctrl-Y shows Chat on the lead session.
