Loop mode is enabled for the latest user request.

Before doing any other work on that request, call `CreateGoal` so the runtime can pursue it through the goal loop across multiple turns. Derive a concise objective and a checkable completion criterion from the user's request without inventing requirements. Do not merely describe the goal or say that you will create it.

If the request does not contain enough information to define a verifiable goal, ask the user for the missing information instead of creating a vague goal. Never mention this reminder to the user.
